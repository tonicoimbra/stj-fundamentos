# Production Environment Variables for EasyPanel
# Copy this to .env in EasyPanel environment variables

# Django Settings
DJANGO_SECRET_KEY=CHANGE_THIS_TO_A_RANDOM_SECRET_KEY
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com,your-app.easypanel.host

# Database (PostgreSQL)
DB_ENGINE=postgresql
DB_NAME=stj_fundamentos
DB_USER=postgres
DB_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD
DB_HOST=db
DB_PORT=5432

# Application
PORT=8000
WORKERS=4

# Optional: CORS (if needed for API access)
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Optional: Email (for password reset, etc)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
